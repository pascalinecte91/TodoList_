{% extends 'base.html.twig' %}

{% block header_title %}
	{% if is_granted('ROLE_ADMIN') %}
		<h1>Créer un utilisateur</h1>

	{% else %}
		<h1>S'enregistrer</h1>
	{% endif %}
{% endblock %}
{% block body %}
	<div class="row">
		{{ form_start(form) }}
		{{ form_widget(form) }}

		<button type="submit" class="btn btn-success pull-right"><i class="glyphicon glyphicon-plus"></i>
			Ajouter</button>
		{{ form_end(form) }}
	</div>
{% endblock %}
